<project name="ch.tweaklab.player" default="dist" basedir=".">

	<presetdef name="javac">
		<javac includeantruntime="false" />
	</presetdef>

	<!-- set global properties for this build -->
	<property name="src" location="src"/>
	<property name="build" location="build"/>
	<property name="dist" location="dist"/>
	<property name="lib" location="lib"/>


	<path id="class.path">
		<dirset dir="${build}" />
		<fileset dir="${lib}"  />

	</path>

	<target name="init">
		<!-- Create the time stamp -->
		<tstamp/>
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}"/>
		<mkdir dir="${build}/lib"/>
		<copy todir="${build}/lib">
			<fileset dir="${lib}" />

		</copy>
	</target>

	<target name="compile" depends="init"
        description="compile the source">
		<!-- Compile the java code from ${src} into ${build} -->
		<javac classpathref="class.path" srcdir="${src}" destdir="${build}"/>
	</target>

	<target name="dist" depends="compile"
        description="generate the distribution">
		<!-- Create the distribution directory -->


		<!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
		<jar destfile="${dist}/tl-player.jar" >
			<fileset dir="${build}"/>
			<fileset dir="${src}" includes="**/*.fxml"/>
			<manifest>
				<attribute name="Main-Class" value="ch.tweaklab.player.gui.controller.MainApp"/>
			</manifest>
		</jar>
	</target>

	<target name="clean"
        description="clean up">
		<delete dir="${build}"/>

	</target>
</project>