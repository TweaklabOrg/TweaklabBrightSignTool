<project name="ch.tweaklab.player" default="clean" basedir=".">

	<presetdef name="javac">
		<javac includeantruntime="false" />
	</presetdef>

	<property name="src.dir" location="src"/>
	<property name="build.dir" location="build"/>
	<property name="dist.dir" location="dist"/>
	<property name="res.dir" location="resources"/>
	<property name="lib.dir" location="lib"/>

	<path id="class.path">
		<dirset dir="${build.dir}" />
		<fileset dir="${lib.dir}"  />
	</path>

	<target name="init">
		<mkdir dir="${build.dir}"/>
		<mkdir dir="${build.dir}/classes"/>
		<mkdir dir="${build.dir}/zip"/>
		<mkdir dir="${build.dir}/zip/work"/>
		<mkdir dir="${build.dir}/zip/bs-scripts"/>
	</target>

	<target name="compile" depends="init"
        description="compile the source">
		<javac classpathref="class.path" srcdir="${src.dir}" destdir="${build.dir}/classes"/>
	</target>

	<target name="jar" depends="compile"
        description="generate the distribution">
		<jar destfile="${build.dir}/zip/tl-player.jar" >
			<fileset dir="${build.dir}/classes"/>
			<fileset dir="${src.dir}" includes="**/*.fxml"/>
			<zipgroupfileset dir="${lib.dir}" includes="*.jar"/>
			
			 <manifest>
				<attribute name="Main-Class" value="ch.tweaklab.player.gui.controller.MainApp"/>
			</manifest>
		</jar>
	</target>

	<target name="zip" depends="jar" description="Creates inst zip">
		<copy todir="${build.dir}/zip/">
			<fileset dir="${res.dir}/misc" />
		</copy>
		<copy todir="${build.dir}/zip/bs-scripts">
			<fileset dir="${res.dir}/bs-scripts"/>
		</copy>
		<copy todir="${build.dir}/zip/">
			<fileset dir="${res.dir}/" includes="**/*properties"/>
		</copy>

		<zip destfile="${dist.dir}/player-client.zip">
			<fileset dir="${build.dir}/zip">
			</fileset>
		</zip>
	</target>

	<target name="clean" depends="zip" description="clean up">
		<delete dir="${build.dir}"/>
	</target>
</project>